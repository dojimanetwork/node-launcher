apiVersion: v1
kind: Service
metadata:
  name: {{ include "landing-page-daemon.fullname" . }}
  labels:
    {{- include "landing-page-daemon.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ include "landing-page-daemon.http" . }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "landing-page-daemon.selectorLabels" . | nindent 4 }}
