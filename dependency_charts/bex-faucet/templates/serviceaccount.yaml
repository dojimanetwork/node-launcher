{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "bex-faucet.serviceAccountName" . }}
  labels:
{{ include "bex-faucet.labels" . | nindent 4 }}
{{- end -}}
