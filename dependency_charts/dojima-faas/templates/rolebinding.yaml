{{- if .Values.serviceAccount.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "dojima-faas-daemon.fullname" . }}
  labels:
{{ include "dojima-faas-daemon.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "dojima-faas-daemon.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "dojima-faas-daemon.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end -}}
