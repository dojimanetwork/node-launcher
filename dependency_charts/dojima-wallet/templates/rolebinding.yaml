{{- if .Values.serviceAccount.create -}}
apiVersion: rbac.authorization.k8s.io/v1
kind: RoleBinding
metadata:
  name: {{ include "wallet-daemon.fullname" . }}
  labels:
{{ include "wallet-daemon.labels" . | nindent 4 }}
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: Role
  name: {{ include "wallet-daemon.fullname" . }}
subjects:
  - kind: ServiceAccount
    name: {{ include "wallet-daemon.serviceAccountName" . }}
    namespace: {{ .Release.Namespace }}
{{- end -}}
